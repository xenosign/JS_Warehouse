function calculate(a, b) {
  const result = a ** 2 + b ** 3 + a * b + a / b;
  return result;
}

const num1 = 1;
const num2 = 2;
const result1 = calculate(num1, num2);

const num3 = 3;
const num4 = 4;
const result2 = calculate(num3, num4);

const num5 = 5;
const num6 = 6;
const result3 = calculate(num5, num6);

// 자바 스크립트에서 제곱근 구하기, Math.sqrt(숫자)
console.log(Math.sqrt(9)); // 3

function average(num1, num2) {
  // 정답 코드 작성!
}
